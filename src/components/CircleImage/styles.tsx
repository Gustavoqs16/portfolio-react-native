import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        width: 24,
        height: 24,
        position: 'relative',
        zIndex: 1
    },
    image: {
        maxWidth: 24,
        maxHeight: 24,
        borderRadius: 20,
    }
});

export default styles;